<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
  <!-- Allow cleartext only to specific domains. Prefer HTTPS in production. -->
  <domain-config cleartextTrafficPermitted="true">
    <domain includeSubdomains="true">botadmin.shop</domain>
  </domain-config>
</network-security-config>

